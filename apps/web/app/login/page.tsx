'use client';
import { useState } from 'react';
export default function LoginPage() { const [email,setEmail]=useState(''); const [sent,setSent]=useState(false); const submit=(e:React.FormEvent)=>{e.preventDefault(); localStorage.setItem('chessapex:preview-profile',email); setSent(true);}; return <main className="shell"><section className="content" style={{maxWidth:520}}><div className="brand">Chess<span style={{color:'var(--lime)'}}>Apex</span></div><div className="card login-card"><div className="eyebrow">Enter the apex</div><h1>Save your climb.</h1><p>This preview stores a local profile on your device. Production account sync will use the Neon boundary without exposing database credentials to the browser.</p>{sent?<p className="success">Preview profile saved for {email}.</p>:<form onSubmit={submit}><input required type="email" placeholder="you@example.com" value={email} onChange={e=>setEmail(e.target.value)}/><button className="cta">Create profile <span>↗</span></button></form>}</div></section></main>; }
