'use client';
import { useState } from 'react';
export default function LoginPage() { const [email,setEmail]=useState(''); const [sent,setSent]=useState(false); const submit=(e:React.FormEvent)=>{e.preventDefault(); setSent(true);}; return <main className="shell"><section className="content" style={{maxWidth:520}}><div className="brand">Chess<span style={{color:'var(--lime)'}}>Apex</span></div><div className="card login-card"><div className="eyebrow">Enter the apex</div><h1>Save your climb.</h1><p>ChessApex uses Neon for progress, games, and lesson history. Authentication stays behind this clean account boundary so the database is never exposed to the browser.</p>{sent?<p className="success">Profile invite queued for {email}.</p>:<form onSubmit={submit}><input required type="email" placeholder="you@example.com" value={email} onChange={e=>setEmail(e.target.value)}/><button className="cta">Create profile <span>↗</span></button></form>}</div></section></main>; }
